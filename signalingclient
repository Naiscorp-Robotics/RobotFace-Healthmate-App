#include "signalingclient"

#include <utility>

class signalingclientData : public QSharedData
{
public:
};

signalingclient::signalingclient()
    : data(new signalingclientData)
{}

signalingclient::signalingclient(const signalingclient &rhs)
    : data{rhs.data}
{}

signalingclient::signalingclient(signalingclient &&rhs)
    : data{std::move(rhs.data)}
{}

signalingclient &signalingclient::operator=(const signalingclient &rhs)
{
    if (this != &rhs)
        data = rhs.data;
    return *this;
}

signalingclient &signalingclient::operator=(signalingclient &&rhs)
{
    if (this != &rhs)
        data = std::move(rhs.data);
    return *this;
}

signalingclient::~signalingclient() {}
